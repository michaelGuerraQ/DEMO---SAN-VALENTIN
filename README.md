DEMO - San Valentín

Este es un proyecto de demostración con una plantilla para San Valentín. Desarrollado con Vite, React y TypeScript, listo para personalizar y desplegar.

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Run the app:
   `npm run dev`
